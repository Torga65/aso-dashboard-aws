version: 1

# ---------------------------------------------------------------------------
# Amplify Gen 2 build spec
# Backend is deployed by the pipeline (ampx pipeline-deploy),
# not by the Amplify Hosting build job — this file only builds the frontend.
# Node 20 LTS is pinned so the lock file and CI use the same npm version.
# ---------------------------------------------------------------------------

backend:
  phases:
    build:
      commands:
        - nvm install 20 && nvm use 20
        - npm install --legacy-peer-deps
        - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID

frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20 && nvm use 20
        - npm install --legacy-peer-deps
    build:
      commands:
        # Write Amplify outputs so the Next.js build can read them
        - npx ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - .next/cache/**/*
      - node_modules/**/*
  environment:
    variables:
      NEXT_TELEMETRY_DISABLED: "1"
